public enum LevelNames
{
    GameMenu = 0,
    Town = 1,
    SoulForge = 2
}

public enum SpellLayers
{
    Ability = 11,
    PlayerAbility = 12,
    EnemyAbility = 13,
}

public enum NpcLayers
{
    Player = 6,
    Golem = 7,
    Enemy = 8,
    NPC = 9,
}

public enum ObjectLayers
{
    Wall = 15,
    Interactable = 16,
}